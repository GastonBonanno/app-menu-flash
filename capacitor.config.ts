import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amagaspa.menuFlash',
  appName: 'app-menu-flash',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
